/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://konsuki.github.io/replab', // あなたのサイトのURL
  generateRobotsTxt: true, // robots.txtも自動生成する
  outDir: 'out', // create-next-appのoutput: 'export'先であるoutフォルダに出力する
}